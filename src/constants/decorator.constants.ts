export enum EDecoratorKeyConvert {
  NUMBER = 'convert:number',
  BOOLEAN = 'convert:boolean',
  ARRAY_NUMBER = 'convert:array_number',
  ARRAY_STRING = 'convert:array_string',
}

export enum EDecoratorError {
  HTTP = 'error:http',
}

